{
  "name": "project-backend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "scripts": {
    "start": "json-server --watch db.json --port #PORT"
  },
  "engines": {
    "node": ">=14"
  }
}
